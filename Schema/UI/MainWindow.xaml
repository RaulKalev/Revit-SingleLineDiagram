<Window
    x:Class="Schema.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Schema"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    Title="Schema drawing"
    Width="1450"
    Height="900"
    MinWidth="1450"
    MinHeight="900"
    AllowsTransparency="True"
    Background="Transparent"
    KeyDown="Window_KeyDown"
    Loaded="Window_Loaded"
    Topmost="True"
    WindowStyle="None">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!--  Material Design default theme  -->
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.ComboBox.xaml" />
                <!--  Material Design dark theme  -->
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Dark.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <!--  Custom Style for DataGrid Header  -->
            <Style x:Key="CustomDataGridColumnHeaderStyle" TargetType="{x:Type DataGridColumnHeader}">
                <Setter Property="Background" Value="{DynamicResource SecondaryBackgroundBrush}" />
                <!--  Set your desired background color  -->
                <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
                <!--  Set your desired foreground color  -->
                <Setter Property="FontWeight" Value="Bold" />
                <Setter Property="HorizontalContentAlignment" Value="Center" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="Padding" Value="5" />
                <Setter Property="BorderBrush" Value="Gray" />
                <Setter Property="BorderThickness" Value="0,0,1,1" />
            </Style>
            <!--  Custom Style for Center-Aligned Cell Content  -->
            <Style x:Key="CenterAlignedCellStyle" TargetType="{x:Type TextBlock}">
                <Setter Property="TextAlignment" Value="Center" />
                <Setter Property="VerticalAlignment" Value="Center" />
            </Style>
            <!--  Custom Style for DataGrid  -->
            <Style
                x:Key="CustomDataGridStyle"
                BasedOn="{StaticResource {x:Type DataGrid}}"
                TargetType="{x:Type DataGrid}">
                <Setter Property="SelectionMode" Value="Single" />
                <Setter Property="SelectionUnit" Value="FullRow" />
            </Style>
            <!--  Global DarkThemeComboBox  -->
            <Style
                x:Key="DarkThemeComboBox"
                BasedOn="{StaticResource MaterialDesignComboBox}"
                TargetType="{x:Type ComboBox}">
                <!--  Base Properties  -->
                <Setter Property="Background" Value="{DynamicResource SecondaryBackgroundBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Padding" Value="2,0,2,0" />
                <Setter Property="FontSize" Value="12" />
                <Setter Property="HorizontalContentAlignment" Value="Center" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="materialDesign:HintAssist.IsFloating" Value="True" />
                <Setter Property="materialDesign:HintAssist.Foreground" Value="{DynamicResource ForegroundBrush}" />
                <!--  ItemContainerStyle for ComboBox Items  -->
                <Setter Property="ItemContainerStyle">
                    <Setter.Value>
                        <Style TargetType="{x:Type ComboBoxItem}">
                            <Setter Property="Padding" Value="0,2,0,2" />
                            <Setter Property="Margin" Value="0,1,0,1" />
                            <Setter Property="HorizontalContentAlignment" Value="Center" />
                            <Setter Property="VerticalContentAlignment" Value="Center" />
                            <Setter Property="Background" Value="{DynamicResource SecondaryBackgroundBrush}" />
                            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
                            <Setter Property="BorderThickness" Value="0" />
                            <!--  Triggers for ComboBoxItem  -->
                            <Style.Triggers>
                                <!--  Mouse Over Trigger  -->
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource HighlightBrush}" />
                                    <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
                                </Trigger>
                                <!--  Selected Trigger  -->
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource HighlightBrush}" />
                                    <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Setter.Value>
                </Setter>
                <!--  ControlTemplate for ComboBox  -->
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ComboBox}">
                            <Grid>
                                <!--  ComboBox Content  -->
                                <ToggleButton
                                    x:Name="ToggleButton"
                                    HorizontalContentAlignment="Left"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    FocusVisualStyle="{x:Null}"
                                    Foreground="{TemplateBinding Foreground}"
                                    IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource Mode=TemplatedParent}}">
                                    <ContentPresenter
                                        Margin="{TemplateBinding Padding}"
                                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                        VerticalAlignment="Center"
                                        Content="{TemplateBinding SelectionBoxItem}"
                                        ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                        ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" />
                                </ToggleButton>
                                <!--  Dropdown Popup  -->
                                <Popup
                                    x:Name="Popup"
                                    AllowsTransparency="True"
                                    Focusable="False"
                                    IsOpen="{TemplateBinding IsDropDownOpen}"
                                    Placement="Bottom">
                                    <Border
                                        x:Name="PopupBorder"
                                        Width="{Binding ActualWidth, RelativeSource={RelativeSource Mode=TemplatedParent}}"
                                        Background="{DynamicResource SecondaryBackgroundBrush}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}">
                                        <ScrollViewer Margin="4,6,4,6">
                                            <StackPanel IsItemsHost="True" />
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                            <!--  Triggers for ComboBox  -->
                            <ControlTemplate.Triggers>
                                <!--  Disabled State Trigger  -->
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Foreground" Value="Gray" />
                                </Trigger>
                                <!--  Mouse Over Trigger (Change Background to Red)  -->
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="ToggleButton" Property="Background" Value="Red" />
                                    <Setter TargetName="ToggleButton" Property="BorderBrush" Value="Red" />
                                </Trigger>
                                <!--  DropDown Open Trigger  -->
                                <Trigger Property="IsDropDownOpen" Value="True">
                                    <Setter TargetName="ToggleButton" Property="Background" Value="{DynamicResource HighlightBrush}" />
                                    <Setter TargetName="ToggleButton" Property="BorderBrush" Value="{DynamicResource HighlightBrush}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style
                x:Key="CustomTextBoxStyle"
                BasedOn="{StaticResource {x:Type TextBox}}"
                TargetType="{x:Type TextBox}">
                <Setter Property="Background" Value="{DynamicResource SecondaryBackgroundBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
                <Setter Property="HorizontalAlignment" Value="Center" />
                <Setter Property="TextAlignment" Value="Right" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Padding" Value="0" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type TextBox}">
                            <Border
                                x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                                <ScrollViewer x:Name="PART_ContentHost" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <!--  Change border color on mouse over  -->
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource SelectionHoverBrush}" />
                                </Trigger>
                                <!--  Change border color on focus  -->
                                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                    <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource TextHighlightBrush}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="CustomActionButtonStyle" TargetType="{x:Type Button}">
                <Setter Property="Width" Value="200" />
                <Setter Property="Height" Value="50" />
                <Setter Property="Margin" Value="0,5,0,0" />
                <Setter Property="HorizontalAlignment" Value="Center" />
                <Setter Property="Background" Value="{DynamicResource PrimaryBackgroundBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
                <Setter Property="FontSize" Value="16" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <Border
                                x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                                <ContentPresenter
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Content="{TemplateBinding Content}" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <!--  Hover Effect  -->
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource SecondaryBackgroundBrush}" />
                                    <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="{DynamicResource IconHoverBrush}" />
                                </Trigger>
                                <!--  Pressed Effect  -->
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource IconHoverBrush}" />
                                    <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style
                x:Key="MultiRowHeaderStyle"
                BasedOn="{StaticResource CustomDataGridColumnHeaderStyle}"
                TargetType="{x:Type DataGridColumnHeader}">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type DataGridColumnHeader}">
                            <Border
                                Background="{TemplateBinding Background}"
                                BorderBrush="Gray"
                                BorderThickness="0,0,1,1">
                                <Grid>
                                    <!--  Main Header Text  -->
                                    <TextBlock
                                        Grid.Row="0"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontWeight="Bold"
                                        Text="{TemplateBinding Content}"
                                        TextAlignment="Center"
                                        TextWrapping="Wrap" />
                                </Grid>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!--  Custom Checkbox Style with Icons  -->
            <Style x:Key="CustomCheckboxStyle" TargetType="{x:Type CheckBox}">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type CheckBox}">
                            <Border
                                Padding="0"
                                Background="Transparent"
                                Cursor="Hand">
                                <Grid Background="Transparent">
                                    <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                                        <!--  Checkbox Icon  -->
                                        <materialDesign:PackIcon
                                            x:Name="CheckboxIcon"
                                            Width="25"
                                            Height="25"
                                            VerticalAlignment="Center"
                                            Foreground="{DynamicResource IconForegroundBrush}"
                                            Kind="CheckboxBlankOutline" />

                                        <!--  Checkbox Text  -->
                                        <TextBlock
                                            Margin="5,0,0,0"
                                            VerticalAlignment="Center"
                                            Foreground="{DynamicResource ForegroundBrush}"
                                            Text="{TemplateBinding Content}" />
                                    </StackPanel>

                                    <!--  Clickable Transparent Overlay  -->
                                    <Border
                                        x:Name="ClickableArea"
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Stretch"
                                        Background="Transparent"
                                        Cursor="Hand" />
                                </Grid>
                            </Border>

                            <ControlTemplate.Triggers>
                                <!--  Checked State  -->
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="CheckboxIcon" Property="Kind" Value="CheckboxMarkedOutline" />
                                    <Setter TargetName="CheckboxIcon" Property="Foreground" Value="{DynamicResource IconForegroundBrush}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!--  Custom Checkbox Style with Icons  -->
            <Style x:Key="CustomCheckboxStyleM" TargetType="{x:Type CheckBox}">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type CheckBox}">
                            <Border
                                Padding="0"
                                Background="Transparent"
                                Cursor="Hand">
                                <Grid Background="Transparent">
                                    <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                                        <!--  Checkbox Icon  -->
                                        <materialDesign:PackIcon
                                            x:Name="CheckboxIcon"
                                            Width="15"
                                            Height="15"
                                            VerticalAlignment="Center"
                                            Foreground="{DynamicResource IconForegroundBrush}"
                                            Kind="CheckboxBlankOutline" />

                                        <!--  Checkbox Text  -->
                                        <TextBlock
                                            Margin="5,0,0,0"
                                            VerticalAlignment="Center"
                                            Foreground="{DynamicResource ForegroundBrush}"
                                            Text="{TemplateBinding Content}" />
                                    </StackPanel>

                                    <!--  Clickable Transparent Overlay  -->
                                    <Border
                                        x:Name="ClickableArea"
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Stretch"
                                        Background="Transparent"
                                        Cursor="Hand" />
                                </Grid>
                            </Border>

                            <ControlTemplate.Triggers>
                                <!--  Checked State  -->
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="CheckboxIcon" Property="Kind" Value="CheckboxMarkedOutline" />
                                    <Setter TargetName="CheckboxIcon" Property="Foreground" Value="{DynamicResource IconForegroundBrush}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Background="Transparent" ClipToBounds="True">
        <!--  Outer Border with Opacity and Blur  -->
        <Border
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="2"
            ClipToBounds="True"
            CornerRadius="10"
            Opacity="0.7"
            SnapsToDevicePixels="True">
            <Border.Effect>
                <BlurEffect KernelType="Gaussian" Radius="2" />
            </Border.Effect>
        </Border>
        <!--  Content Container (Not Affected by Opacity/Blur)  -->
        <Border
            Margin="2,2,2,2"
            Background="{DynamicResource PrimaryBackgroundBrush}"
            CornerRadius="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <!--  main content  -->
                    <ColumnDefinition Width="300" />
                    <!--  right-side panel  -->
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <!--  Title, Levels, Controls, DataGrid  -->
                    <RowDefinition Height="30" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!--  Custom Title Bar  -->
                <local:CustomTitleBar
                    Grid.Row="0"
                    Grid.ColumnSpan="2"
                    Margin="0,0,0,1"
                    Foreground="{DynamicResource ForegroundBrush}" />
                <Grid
                    Grid.Row="0"
                    Grid.Column="0"
                    Grid.ColumnSpan="2"
                    Margin="0,0,100,0"
                    HorizontalAlignment="Right">
                    <ToggleButton
                        x:Name="ThemeToggleButton"
                        Width="28"
                        Height="15.5"
                        Background="Transparent"
                        BorderBrush="Transparent"
                        Click="ToggleTheme_Click"
                        DockPanel.Dock="Right"
                        IsChecked="True"
                        ToolTip="Toggle Theme">
                        <ToggleButton.Template>
                            <ControlTemplate TargetType="ToggleButton">
                                <Grid Background="Transparent">
                                    <!--  Dynamic Icon Based on Toggle State  -->
                                    <materialDesign:PackIcon
                                        x:Name="ThemeToggleIcon"
                                        Width="30"
                                        Height="30"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Foreground="{DynamicResource IconBrush}"
                                        Kind="ToggleSwitchOffOutline" />
                                </Grid>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsChecked" Value="True">
                                        <Setter TargetName="ThemeToggleIcon" Property="Kind" Value="ToggleSwitchOffOutline" />
                                    </Trigger>
                                    <Trigger Property="IsChecked" Value="False">
                                        <Setter TargetName="ThemeToggleIcon" Property="Kind" Value="ToggleSwitchOutline" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </ToggleButton.Template>
                    </ToggleButton>
                </Grid>

                <!--  Main Content Area  -->
                <Grid
                    Grid.Row="1"
                    Margin="10,0,0,10"
                    Background="{DynamicResource PrimaryBackgroundBrush}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <!--  View selection & levels  -->
                        <RowDefinition Height="*" />
                        <!--  SystemDataGrid  -->
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Orientation="Vertical">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock
                                x:Name="BoundaryInfoTextBlock"
                                Margin="10,7,10,10"
                                VerticalAlignment="Center"
                                FontSize="14"
                                Foreground="{DynamicResource ForegroundBrush}"
                                Text="Select a drafting view..."
                                TextWrapping="Wrap" />
                            <TextBlock
                                x:Name="SelectDraftingView"
                                Margin="50,7,0,10"
                                VerticalAlignment="Center"
                                FontSize="14"
                                Foreground="{DynamicResource ForegroundBrush}"
                                Text="Drafting View:"
                                TextWrapping="Wrap" />
                            <ComboBox
                                x:Name="ViewComboBox"
                                Width="250"
                                Margin="10,0,0,0"
                                Padding="4"
                                VerticalContentAlignment="Center"
                                materialDesign:HintAssist.Hint="Select View"
                                DisplayMemberPath="Name"
                                SelectionChanged="ViewComboBox_SelectionChanged"
                                Style="{StaticResource DarkThemeComboBox}" />
                        </StackPanel>

                        <DataGrid
                            x:Name="LevelsDataGrid"
                            Grid.Row="1"
                            Margin="0,10,0,10"
                            AllowDrop="True"
                            AlternatingRowBackground="{DynamicResource SecondaryBackgroundBrush}"
                            AutoGenerateColumns="False"
                            Background="{DynamicResource SecondaryBackgroundBrush}"
                            BorderBrush="Gray"
                            CanUserAddRows="True"
                            CanUserResizeColumns="True"
                            CanUserResizeRows="False"
                            CanUserSortColumns="False"
                            ColumnHeaderStyle="{StaticResource CustomDataGridColumnHeaderStyle}"
                            Foreground="{DynamicResource ForegroundBrush}"
                            GridLinesVisibility="Horizontal"
                            HorizontalScrollBarVisibility="Hidden"
                            IsReadOnly="False"
                            ItemsSource="{Binding Levels}"
                            RowBackground="{DynamicResource PrimaryBackgroundBrush}"
                            RowHeight="20"
                            SelectionMode="Extended"
                            SelectionUnit="FullRow"
                            Style="{StaticResource CustomDataGridStyle}"
                            VerticalScrollBarVisibility="Auto">
                            <DataGrid.Columns>
                                <DataGridTemplateColumn Width="40" Header="✓">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <CheckBox HorizontalAlignment="Center" IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>


                                <DataGridTextColumn
                                    Width="*"
                                    Binding="{Binding Name}"
                                    Header="Level Name" />
                                <DataGridTextColumn
                                    Width="120"
                                    Binding="{Binding Elevation}"
                                    Header="Elevation (mm)" />
                            </DataGrid.Columns>
                        </DataGrid>
                        <StackPanel />
                    </StackPanel>

                    <DataGrid
                        x:Name="SystemDataGrid"
                        Grid.Row="1"
                        Margin="0,10,0,0"
                        VerticalAlignment="Top"
                        AllowDrop="True"
                        AlternatingRowBackground="{DynamicResource SecondaryBackgroundBrush}"
                        AutoGenerateColumns="False"
                        Background="{DynamicResource SecondaryBackgroundBrush}"
                        BorderBrush="Gray"
                        CanUserAddRows="True"
                        CanUserResizeColumns="True"
                        CanUserResizeRows="False"
                        CanUserSortColumns="False"
                        ColumnHeaderStyle="{StaticResource CustomDataGridColumnHeaderStyle}"
                        Foreground="{DynamicResource ForegroundBrush}"
                        GridLinesVisibility="Horizontal"
                        HeadersVisibility="Column"
                        HorizontalScrollBarVisibility="Hidden"
                        IsReadOnly="False"
                        ItemsSource="{Binding FireAlarmElements}"
                        RowBackground="{DynamicResource PrimaryBackgroundBrush}"
                        RowHeight="20"
                        SelectionMode="Extended"
                        SelectionUnit="FullRow"
                        Style="{StaticResource CustomDataGridStyle}"
                        VerticalScrollBarVisibility="Auto">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Width="40" Header="✓">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox HorizontalAlignment="Center" IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn
                                Width="60"
                                Binding="{Binding SectionNumber, UpdateSourceTrigger=PropertyChanged}"
                                Header="Section"
                                IsReadOnly="False">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Center" />
                                        <Setter Property="VerticalAlignment" Value="Center" />
                                        <Setter Property="TextAlignment" Value="Center" />
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                                <DataGridTextColumn.EditingElementStyle>
                                    <Style TargetType="TextBox">
                                        <Setter Property="HorizontalContentAlignment" Value="Center" />
                                        <Setter Property="VerticalContentAlignment" Value="Center" />
                                        <Setter Property="TextAlignment" Value="Center" />
                                    </Style>
                                </DataGridTextColumn.EditingElementStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn
                                Width="200"
                                Binding="{Binding FamilyName}"
                                Header="Family" />
                            <DataGridTextColumn
                                Width="300"
                                Binding="{Binding TypeName}"
                                Header="Type" />
                            <DataGridTextColumn
                                Width="100"
                                Binding="{Binding LevelName}"
                                Header="Level" />
                            <DataGridTextColumn
                                Width="50"
                                Binding="{Binding AhelaNr}"
                                Header="Ahel"
                                IsReadOnly="True" />
                            <DataGridTextColumn Binding="{Binding Aadress}" Header="Aadress" />

                            <DataGridTextColumn
                                Width="50"
                                Binding="{Binding Count}"
                                Header="Count" />
                            <DataGridComboBoxColumn
                                Width="*"
                                DisplayMemberPath="DisplayName"
                                Header="Detail Symbol"
                                SelectedItemBinding="{Binding SelectedDetailType}">
                                <DataGridComboBoxColumn.ElementStyle>
                                    <Style TargetType="ComboBox">
                                        <Setter Property="ItemsSource" Value="{Binding AvailableDetailTypes}" />
                                    </Style>
                                </DataGridComboBoxColumn.ElementStyle>
                                <DataGridComboBoxColumn.EditingElementStyle>
                                    <Style TargetType="ComboBox">
                                        <Setter Property="ItemsSource" Value="{Binding AvailableDetailTypes}" />
                                    </Style>
                                </DataGridComboBoxColumn.EditingElementStyle>
                            </DataGridComboBoxColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>


                <!--  Right side panel  -->
                <Border
                    Grid.Row="1"
                    Grid.Column="1"
                    Margin="10"
                    Padding="10"
                    Background="{DynamicResource SecondaryBackgroundBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="5">
                    <StackPanel>
                        <StackPanel
                            Margin="0,0,0,10"
                            HorizontalAlignment="Center"
                            Orientation="Horizontal">
                            <!--  PP Checkbox  -->
                            <CheckBox
                                x:Name="PPCheckBox"
                                Checked="PPCheckBox_Checked"
                                Content="PP"
                                IsChecked="True"
                                Style="{StaticResource CustomCheckboxStyle}"
                                ToolTip="Põhiprojekt"
                                Unchecked="PPCheckBox_Unchecked" />
                            <!--  TP Checkbox  -->
                            <CheckBox
                                x:Name="TPCheckBox"
                                Margin="10,0,0,0"
                                Checked="TPCheckBox_Checked"
                                Content="TP"
                                IsChecked="False"
                                Style="{StaticResource CustomCheckboxStyle}"
                                ToolTip="Tööprojekt"
                                Unchecked="TPCheckBox_Unchecked" />
                        </StackPanel>
                        <StackPanel
                            Margin="0,0,0,10"
                            HorizontalAlignment="Center"
                            Orientation="Horizontal">
                            <CheckBox
                                x:Name="ATSCheckBox"
                                Checked="ATSCheckBox_CheckedChanged"
                                Content="ATS"
                                IsChecked="True"
                                Style="{StaticResource CustomCheckboxStyleM}"
                                Unchecked="ATSCheckBox_CheckedChanged" />
                            <CheckBox
                                x:Name="VVSCheckBox"
                                Margin="10,0,0,0"
                                Checked="VVSCheckBox_CheckedChanged"
                                Content="VVS"
                                IsChecked="False"
                                Style="{StaticResource CustomCheckboxStyleM}"
                                Unchecked="VVSCheckBox_CheckedChanged" />

                            <CheckBox
                                x:Name="LPSCheckBox"
                                Margin="10,0,0,0"
                                Checked="LPSCheckBox_CheckedChanged"
                                Content="LPS"
                                IsChecked="False"
                                Style="{StaticResource CustomCheckboxStyleM}"
                                Unchecked="LPSCheckBox_CheckedChanged" />

                            <CheckBox
                                x:Name="SHSCheckBox"
                                Margin="10,0,0,0"
                                Checked="SHSCheckBox_CheckedChanged"
                                Content="SHS"
                                IsChecked="False"
                                Style="{StaticResource CustomCheckboxStyleM}"
                                Unchecked="SHSCheckBox_CheckedChanged" />

                            <CheckBox
                                x:Name="SideCheckBox"
                                Margin="10,0,0,0"
                                Checked="SideCheckBox_CheckedChanged"
                                Content="Side"
                                IsChecked="False"
                                Style="{StaticResource CustomCheckboxStyleM}"
                                Unchecked="SideCheckBox_CheckedChanged" />

                        </StackPanel>


                        <TextBlock
                            Margin="0,0,0,10"
                            FontSize="16"
                            FontWeight="Bold"
                            Foreground="{DynamicResource ForegroundBrush}"
                            Text="Drawing area options:" />
                        <StackPanel>
                            <TextBlock
                                Margin="0,0,0,0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                FontSize="14"
                                Foreground="{DynamicResource ForegroundBrush}"
                                Text="Margin from edge (mm):" />
                            <TextBox
                                Name="MarginTextBox"
                                Width="60"
                                Margin="10,-19,0,0"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Style="{StaticResource CustomTextBoxStyle}"
                                Text="10" />
                        </StackPanel>
                        <TextBlock
                            Margin="0,10,0,10"
                            FontSize="16"
                            FontWeight="Bold"
                            Foreground="{DynamicResource ForegroundBrush}"
                            Text="Detail item options:" />
                        <StackPanel>
                            <TextBlock
                                Margin="0,0,0,0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                FontSize="14"
                                Foreground="{DynamicResource ForegroundBrush}"
                                Text="Start Offset left (mm):" />
                            <TextBox
                                x:Name="StartOffsetTextBox"
                                Width="60"
                                Margin="10,-19,0,0"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Style="{StaticResource CustomTextBoxStyle}"
                                Text="10" />

                            <TextBlock
                                Margin="0,10,0,0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                FontSize="14"
                                Foreground="{DynamicResource ForegroundBrush}"
                                Text="Start Offset right (mm):" />
                            <TextBox
                                Name="RightMarginTextBox"
                                Width="60"
                                Margin="10,-19,0,0"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Style="{StaticResource CustomTextBoxStyle}"
                                Text="10" />

                            <TextBlock
                                Margin="0,10,0,0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                FontSize="14"
                                Foreground="{DynamicResource ForegroundBrush}"
                                Text="Verical offset + (mm):" />
                            <TextBox
                                Name="VerticalMarginTextBox"
                                Width="60"
                                Margin="10,-19,0,0"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Style="{StaticResource CustomTextBoxStyle}"
                                Text="0" />

                            <TextBlock
                                Margin="0,10,0,0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                FontSize="14"
                                Foreground="{DynamicResource ForegroundBrush}"
                                Text="Spacing (mm):" />
                            <TextBox
                                x:Name="SpacingTextBox"
                                Width="60"
                                Margin="10,-19,0,0"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Style="{StaticResource CustomTextBoxStyle}"
                                Text="10" />

                            <TextBlock
                                Margin="0,10,0,0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                FontSize="14"
                                Foreground="{DynamicResource ForegroundBrush}"
                                Text="Max. Detail items in row:" />
                            <TextBox
                                x:Name="ItemsInRow"
                                Width="60"
                                Margin="10,-19,0,0"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Style="{StaticResource CustomTextBoxStyle}"
                                Text="20" />

                            <TextBlock
                                Margin="0,10,0,0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                FontSize="14"
                                Foreground="{DynamicResource ForegroundBrush}"
                                Text="Row ofset:" />
                            <TextBox
                                x:Name="RowOffset"
                                Width="60"
                                Margin="10,-19,0,0"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Style="{StaticResource CustomTextBoxStyle}"
                                Text="500" />
                        </StackPanel>
                        <TextBlock
                            Margin="0,10,0,10"
                            FontSize="16"
                            FontWeight="Bold"
                            Foreground="{DynamicResource ForegroundBrush}"
                            Text="Tag options:" />
                        <StackPanel>
                            <TextBlock
                                Margin="0,0,0,0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                FontSize="14"
                                Foreground="{DynamicResource ForegroundBrush}"
                                Text="Top tag offset:" />
                            <TextBox
                                x:Name="TagOffsetTextBox"
                                Width="60"
                                Margin="10,-19,0,0"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Style="{StaticResource CustomTextBoxStyle}"
                                Text="150"
                                ToolTip="Offset from symbol in mm" />

                            <TextBlock
                                Margin="0,10,0,0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                FontSize="14"
                                Foreground="{DynamicResource ForegroundBrush}"
                                Text="Tag parameter:" />
                            <TextBox
                                x:Name="TagParamater"
                                Width="120"
                                Margin="10,-19,0,0"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Style="{StaticResource CustomTextBoxStyle}"
                                Text="Comments"
                                ToolTip="Set the paramater you want to tag." />

                            <TextBlock
                                Margin="0,10,0,0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                FontSize="14"
                                Foreground="{DynamicResource ForegroundBrush}"
                                Text="Top tag type:" />
                            <ComboBox
                                x:Name="TagTypeComboBox"
                                Margin="0,10,0,0"
                                DisplayMemberPath="Name"
                                SelectedIndex="0"
                                Style="{StaticResource DarkThemeComboBox}"
                                ToolTip="Select a tag type to apply to placed items" />

                        </StackPanel>
                        <TextBlock
                            Margin="0,10,0,10"
                            FontSize="16"
                            FontWeight="Bold"
                            Foreground="{DynamicResource ForegroundBrush}"
                            Text="Controller options:" />
                        <StackPanel>
                            <TextBlock
                                Margin="0,0,0,0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                FontSize="14"
                                Foreground="{DynamicResource ForegroundBrush}"
                                Text="Controller detail offset:" />
                            <TextBox
                                x:Name="ControllerOffsetTextBox"
                                Width="60"
                                Margin="10,-19,0,0"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Style="{StaticResource CustomTextBoxStyle}"
                                Text="150" />

                            <TextBlock
                                Margin="0,10,0,0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                FontSize="14"
                                Foreground="{DynamicResource ForegroundBrush}"
                                Text="Controller detail type:" />
                            <ComboBox
                                x:Name="ControllerTypeComboBox"
                                Margin="0,10,0,0"
                                DisplayMemberPath="DisplayName"
                                SelectedIndex="0"
                                Style="{StaticResource DarkThemeComboBox}" />

                        </StackPanel>
                        <StackPanel VerticalAlignment="Bottom">
                            <Button
                                Height="30"
                                Margin="0,20,0,0"
                                Click="DrawLinesButton_Click"
                                Content="Draw Lines"
                                Style="{StaticResource CustomActionButtonStyle}" />
                            <Button
                                Height="30"
                                Margin="0,20,0,0"
                                Click="PlaceDetailsButton_Click"
                                Content="Place"
                                Style="{StaticResource CustomActionButtonStyle}" />
                            <Button
                                Height="30"
                                Margin="0,20,0,0"
                                Click="PlaceKSButton_Click"
                                Content="Place KS"
                                Style="{StaticResource CustomActionButtonStyle}" />
                            <Button
                                Height="30"
                                Margin="0,20,0,0"
                                Click="DrawLines_Click"
                                Content="Draw Level Lines"
                                Style="{StaticResource CustomActionButtonStyle}" />
                        </StackPanel>
                        <!--  MainWindow.xaml  -->
                        <StackPanel Margin="0,10,0,0" Orientation="Horizontal">
                            <TextBox
                                x:Name="BulkValueTextBox"
                                Width="130"
                                Margin="5"
                                Style="{StaticResource CustomTextBoxStyle}" />
                            <Button
                                Width="100"
                                Height="25"
                                Margin="18,0,0,0"
                                Click="SetSectionNumber_Click"
                                Content="Set Section Number"
                                FontSize="10"
                                Style="{StaticResource CustomActionButtonStyle}" />
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
        <!--  Left Edge  -->
        <Border
            x:Name="LeftEdge"
            Width="5"
            HorizontalAlignment="Left"
            Background="Transparent"
            Cursor="SizeWE"
            MouseEnter="LeftEdge_MouseEnter"
            MouseLeave="Edge_MouseLeave"
            MouseLeftButtonDown="LeftEdge_MouseLeftButtonDown" />
        <!--  Right Edge  -->
        <Border
            x:Name="RightEdge"
            Width="5"
            HorizontalAlignment="Right"
            Background="Transparent"
            Cursor="SizeWE"
            MouseEnter="RightEdge_MouseEnter"
            MouseLeave="Edge_MouseLeave"
            MouseLeftButtonDown="RightEdge_MouseLeftButtonDown" />
        <!--  Bottom Edge  -->
        <Border
            x:Name="BottomEdge"
            Height="5"
            VerticalAlignment="Bottom"
            Background="Transparent"
            Cursor="SizeNS"
            MouseEnter="BottomEdge_MouseEnter"
            MouseLeave="Edge_MouseLeave"
            MouseLeftButtonDown="BottomEdge_MouseLeftButtonDown" />
        <!--  Bottom-Left Corner  -->
        <Border
            x:Name="BottomLeftCorner"
            Width="10"
            Height="10"
            HorizontalAlignment="Left"
            VerticalAlignment="Bottom"
            Background="Transparent"
            Cursor="SizeNESW"
            MouseEnter="BottomLeftCorner_MouseEnter"
            MouseLeave="Edge_MouseLeave"
            MouseLeftButtonDown="BottomLeftCorner_MouseLeftButtonDown" />
        <!--  Bottom-Right Corner  -->
        <Border
            x:Name="BottomRightCorner"
            Width="10"
            Height="10"
            HorizontalAlignment="Right"
            VerticalAlignment="Bottom"
            Background="Transparent"
            Cursor="SizeNWSE"
            MouseEnter="BottomRightCorner_MouseEnter"
            MouseLeave="Edge_MouseLeave"
            MouseLeftButtonDown="BottomRightCorner_MouseLeftButtonDown" />
    </Grid>
</Window>